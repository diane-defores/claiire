---
import {
  accueilSidebar,
  corpsSidebar,
  violenceSidebar,
} from '../config/navigation';

const navigation = [
  accueilSidebar,
  corpsSidebar,
  violenceSidebar
];
---

{/* Le composant peut retourner la navigation ou être utilisé comme store */} 
